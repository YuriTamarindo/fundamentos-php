<?php
    $name = '  EspecializaTI  ';
    var_dump($name);
    echo '<hr>';
    var_dump(ltrim($name));//retira os espaços do inicio da string
    echo '<hr>';
    var_dump(rtrim($name));//retira os espaços do final da string
    echo '<hr>';
    var_dump(trim($name));//retira os espaços do inicio e do final